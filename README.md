## Structure and Plans

- Once the body content is changed, the table of contents updates dynamically
- Therefore new text content can just be pasted into <a href="src\App.tsx">App.tsx</a>
- For images, I will create a separate `<img>` component, that will make them appear to the side of main text on both sides, whenever there is space, like in books
- For videos a simpler `<video>` component
  Deadline for me: Thursday 15:00

---

- Mobile version by Friday
